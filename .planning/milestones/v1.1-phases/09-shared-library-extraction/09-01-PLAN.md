---
phase: 09-shared-library-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-shared-library-extraction/BOUNDARY.md
  - uv.toml
  - package.json
  - packages/efofx-shared/pyproject.toml
  - packages/efofx-shared/efofx_shared/__init__.py
  - packages/efofx-ui/package.json
  - packages/efofx-ui/tsconfig.json
  - packages/efofx-ui/src/index.ts
autonomous: true
requirements:
  - EXTR-01

must_haves:
  truths:
    - "A boundary document exists with a decision table covering every Python module and frontend component with Location and Rationale columns"
    - "uv workspace config exists at workspace root with apps/efofx-estimate and packages/efofx-shared as members"
    - "npm workspace config exists at workspace root with apps/efofx-widget, apps/efofx-dashboard, and packages/efofx-ui as members"
    - "packages/efofx-shared/ has a valid pyproject.toml with setuptools backend and NO fastapi/motor/uvicorn dependencies"
    - "packages/efofx-ui/ has a valid package.json with react/react-dom as peerDependencies"
  artifacts:
    - path: ".planning/phases/09-shared-library-extraction/BOUNDARY.md"
      provides: "Module-by-module extraction decision table"
      contains: "Module | Location | Rationale"
    - path: "uv.toml"
      provides: "Python workspace config"
      contains: "workspace"
    - path: "package.json"
      provides: "npm workspace config"
      contains: "workspaces"
    - path: "packages/efofx-shared/pyproject.toml"
      provides: "Shared Python package definition"
      contains: "efofx-shared"
    - path: "packages/efofx-ui/package.json"
      provides: "Shared UI package definition"
      contains: "@efofx/ui"
  key_links:
    - from: "uv.toml"
      to: "packages/efofx-shared/pyproject.toml"
      via: "workspace members list"
      pattern: "packages/efofx-shared"
    - from: "package.json"
      to: "packages/efofx-ui/package.json"
      via: "workspaces array"
      pattern: "packages/efofx-ui"
---

<objective>
Create the shared library boundary document and scaffold both workspace packages (Python and TypeScript) so that subsequent plans can extract code into them.

Purpose: Establishes the workspace infrastructure and boundary decisions that all extraction work depends on. The boundary document resolves ambiguous extraction decisions once, preventing per-file debates in later plans.

Output: BOUNDARY.md decision table, uv.toml workspace config, root package.json with npm workspaces, skeleton packages/efofx-shared/ and packages/efofx-ui/ ready for code extraction.
</objective>

<execution_context>
@/Users/brettlee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brettlee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-shared-library-extraction/09-CONTEXT.md
@.planning/phases/09-shared-library-extraction/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create boundary document and workspace configs</name>
  <files>
    .planning/phases/09-shared-library-extraction/BOUNDARY.md
    uv.toml
    package.json
  </files>
  <action>
Create BOUNDARY.md at `.planning/phases/09-shared-library-extraction/BOUNDARY.md` with two decision tables:

**Python Modules Table** — cover every module in `apps/efofx-estimate/app/`:
| Module | Location | Rationale |

Decisions (from research audit):
- `app/utils/crypto.py` → EXTRACT to `packages/efofx-shared/efofx_shared/utils/crypto.py` — pure stdlib + cryptography, no app imports
- `app/core/constants.py` enums only (EstimationStatus, ReferenceClassCategory, CostBreakdownCategory, Region) → EXTRACT to `packages/efofx-shared/efofx_shared/core/constants.py` — pure Python enums, no app imports. API_MESSAGES, ESTIMATION_CONFIG, LLM_PROMPTS, DB_COLLECTIONS, HTTP_STATUS, FILE_UPLOAD_CONFIG stay in app (estimation-specific).
- `app/utils/calculation_utils.py` → STAYS in apps/efofx-estimate — pool construction domain logic (region multipliers, labor costs)
- `app/utils/validation_utils.py` → STAYS — imports app.core.constants (Region, ReferenceClassCategory) which are estimation-specific enums
- `app/utils/file_utils.py` → STAYS — imports fastapi.UploadFile
- `app/models/_objectid.py` → STAYS — imports bson.ObjectId (pymongo dependency)
- All services, API, DB, models modules → STAYS — tightly coupled to FastAPI, Motor, tenant system

**TypeScript Components Table** — cover every component in efofx-widget and efofx-dashboard:
| Component | Location | Rationale |

Decisions (from research audit):
- `ChatBubble.tsx` → EXTRACT to `packages/efofx-ui/` — pure presentational, no hooks
- `EstimateCard.tsx` → EXTRACT — local useState only; define standalone EstimationOutput interface in efofx-ui
- `TypingIndicator.tsx` → EXTRACT — zero props, zero imports
- `ErrorBoundary.tsx` → EXTRACT — generic React error boundary
- `LoadingSkeleton.tsx` (dashboard) → EXTRACT — generic skeleton UI
- `ChatPanel.tsx` → STAYS — uses useChat hook, widget state
- `FloatingButton.tsx` → STAYS — widget-specific
- `ConsultationCTA.tsx` → STAYS — estimation domain
- `ConsultationForm.tsx` → STAYS — estimation domain, useChat
- `LeadCaptureForm.tsx` → STAYS — estimation domain
- `NarrativeStream.tsx` → STAYS — SSE streaming, widget-specific
- `ShadowDOMWrapper.tsx` → STAYS — widget embed mechanism
- All dashboard domain components (AccuracyBucketBar, CalibrationMetrics, etc.) → STAYS — calibration domain, Recharts

Then create workspace configs:

1. Create `uv.toml` at workspace root:
```toml
[workspace]
members = [
    "apps/efofx-estimate",
    "packages/efofx-shared",
]
```

2. Create `package.json` at workspace root:
```json
{
  "name": "efofx-workspace",
  "private": true,
  "workspaces": [
    "apps/efofx-widget",
    "apps/efofx-dashboard",
    "packages/efofx-ui"
  ]
}
```
  </action>
  <verify>
    <automated>test -f .planning/phases/09-shared-library-extraction/BOUNDARY.md && grep -q "Module | Location | Rationale" .planning/phases/09-shared-library-extraction/BOUNDARY.md && test -f uv.toml && grep -q "packages/efofx-shared" uv.toml && test -f package.json && grep -q "packages/efofx-ui" package.json && echo "PASS"</automated>
  </verify>
  <done>BOUNDARY.md contains decision tables for all Python modules and TypeScript components. uv.toml declares Python workspace members. Root package.json declares npm workspace members.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold shared package skeletons</name>
  <files>
    packages/efofx-shared/pyproject.toml
    packages/efofx-shared/efofx_shared/__init__.py
    packages/efofx-shared/efofx_shared/core/__init__.py
    packages/efofx-shared/efofx_shared/utils/__init__.py
    packages/efofx-ui/package.json
    packages/efofx-ui/tsconfig.json
    packages/efofx-ui/src/index.ts
  </files>
  <action>
Create `packages/efofx-shared/` directory structure:

1. `packages/efofx-shared/pyproject.toml`:
```toml
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "efofx-shared"
version = "0.1.0"
description = "Shared utilities for efOfX platform"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.11.0",
    "cryptography>=41.0",
]
```
CRITICAL: NO fastapi, motor, uvicorn, or any app-specific dependency.

2. `packages/efofx-shared/efofx_shared/__init__.py`: Export `__version__ = "0.1.0"`
3. `packages/efofx-shared/efofx_shared/core/__init__.py`: Empty init
4. `packages/efofx-shared/efofx_shared/utils/__init__.py`: Empty init

Create `packages/efofx-ui/` directory structure:

1. `packages/efofx-ui/package.json`:
```json
{
  "name": "@efofx/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "peerDependencies": {
    "react": ">=19.0.0",
    "react-dom": ">=19.0.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2"
  }
}
```

2. `packages/efofx-ui/tsconfig.json` — match efofx-widget's strict settings:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

3. `packages/efofx-ui/src/index.ts`: Empty barrel export file with comment `// Components will be exported here after extraction in plan 09-03`

Run `uv sync` from workspace root to verify Python workspace resolves. Run `npm install` from workspace root to verify npm workspace resolves and creates symlinks.
  </action>
  <verify>
    <automated>test -f packages/efofx-shared/pyproject.toml && test -f packages/efofx-shared/efofx_shared/__init__.py && test -f packages/efofx-ui/package.json && test -f packages/efofx-ui/src/index.ts && ! grep -q "fastapi\|motor\|uvicorn" packages/efofx-shared/pyproject.toml && echo "PASS"</automated>
  </verify>
  <done>Both package skeletons exist with correct configs. efofx-shared has no prohibited dependencies. efofx-ui has react as peerDependency. uv sync and npm install succeed from workspace root.</done>
</task>

</tasks>

<verification>
- BOUNDARY.md exists with module-by-module decision tables
- uv.toml at workspace root with correct members
- Root package.json with workspaces array
- packages/efofx-shared/ skeleton with clean pyproject.toml
- packages/efofx-ui/ skeleton with correct package.json and tsconfig
- `uv sync` succeeds from workspace root
- `npm install` succeeds from workspace root
</verification>

<success_criteria>
- Boundary document resolves extraction decisions for every module and component
- Both workspace systems (uv + npm) are functional
- Package skeletons are ready to receive extracted code
- No prohibited dependencies in shared packages
</success_criteria>

<output>
After completion, create `.planning/phases/09-shared-library-extraction/09-01-SUMMARY.md`
</output>
